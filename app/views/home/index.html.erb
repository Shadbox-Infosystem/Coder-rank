<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Landing Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f0f0f0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .container {
      background-color: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      text-align: center;
    }
    .container h1 {
      font-size: 2rem;
      margin-bottom: 20px;
    }
    .logout-button {
      background-color: #ff4d4d;
      color: #fff;
      padding: 10px 20px;
      font-size: 1rem;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    .logout-button:hover {
      background-color: #e03e3e;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Welcome to the Coding Challenge Platform!</h1>
    <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "logout-button" %>
  </div>

</body>
</html>
